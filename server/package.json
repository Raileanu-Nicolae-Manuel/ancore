{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch run src/index.ts && tsc --watch",
    "push": "bun drizzle-kit push",
    "studio": "bun drizzle-kit studio --port=3002"
  },
  "dependencies": {
    "@libsql/client": "^0.15.9",
    "better-auth": "^1.2.12",
    "better-sqlite3": "^12.2.0",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.7.11",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.2.18",
    "drizzle-kit": "^0.31.4"
  }
}
